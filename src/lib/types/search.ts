export interface CartItem {
    _id: string;
    quantity: number;
    price: number;
    name: string;
    image: string; 
}// Check what CartItem looks like
export interface CartItem {
  // What properties are actually defined here?
}